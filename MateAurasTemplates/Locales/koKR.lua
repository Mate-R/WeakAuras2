if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "koKR" then
  return
end

local L = MateAuras.L

--@localization(locale="koKR", format="lua_additive_table", namespace="MateAuras / Templates")@
