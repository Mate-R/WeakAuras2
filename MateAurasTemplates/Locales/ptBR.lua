if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "ptBR" then
  return
end

local L = MateAuras.L

--@localization(locale="ptBR", format="lua_additive_table", namespace="MateAuras / Templates")@
