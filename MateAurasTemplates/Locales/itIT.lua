if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "itIT" then
  return
end

local L = MateAuras.L

--@localization(locale="itIT", format="lua_additive_table", namespace="MateAuras / Templates")@
