if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "esES" then
  return
end

local L = MateAuras.L

--@localization(locale="esES", format="lua_additive_table", namespace="MateAuras / Templates")@
