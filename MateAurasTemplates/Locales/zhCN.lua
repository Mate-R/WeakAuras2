if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "zhCN" then
  return
end

local L = MateAuras.L

--@localization(locale="zhCN", format="lua_additive_table", namespace="MateAuras / Templates")@
