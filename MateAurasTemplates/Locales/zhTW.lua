if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "zhTW" then
  return
end

local L = MateAuras.L

--@localization(locale="zhTW", format="lua_additive_table", namespace="MateAuras / Templates")@
