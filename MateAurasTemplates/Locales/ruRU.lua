if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "ruRU" then
  return
end

local L = MateAuras.L

--@localization(locale="ruRU", format="lua_additive_table", namespace="MateAuras / Templates")@
