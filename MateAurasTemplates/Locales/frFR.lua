if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "frFR" then
  return
end

local L = MateAuras.L

--@localization(locale="frFR", format="lua_additive_table", namespace="MateAuras / Templates")@
