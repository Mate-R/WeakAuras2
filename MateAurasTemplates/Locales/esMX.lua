if not MateAuras.IsLibsOK() then return end

if (GAME_LOCALE or GetLocale()) ~= "esMX" then
  return
end

local L = MateAuras.L

--@localization(locale="esMX", format="lua_additive_table", namespace="MateAuras / Templates")@
